version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=super-secret-key-12345
      - DATABASE_URL=sqlite:///surveys.db
    volumes:
      - ./surveys.db:/app/surveys.db
      - ./logs:/app/logs